# deps / build
node_modules/
.next/
out/
coverage/
*.tsbuildinfo

# env — .env.example만 커밋 (constitution)
.env
.env.*
!.env.example

# 개인 로컬 상태 — 페이즈·예산·locks 캐시는 커밋하지 않는다
.claude/state/
.claude/settings.local.json

# 런타임 산출물 — 시세 캐시 DB (specs/001 R8)
data/market.db
data/market.db-*

# test artifacts
playwright-report/
test-results/
.stryker-tmp/
reports/
.lighthouseci/
stryker.log
