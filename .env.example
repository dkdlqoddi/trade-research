# 필요한 변수 "이름"만 기록한다 — 값은 절대 커밋 금지 (specs/constitution.md)
# 실제 값은 .env.local(자동 gitignore)에 둔다.

# 피처 플래그 킬스위치: "1"이면 모든 플래그 강제 off (src/lib/flags.ts)
NEXT_PUBLIC_FLAGS_KILL_SWITCH=

# 수집 엔드포인트 보호 토큰(선택) — 설정 시 GET /api/collect 가 Bearer 인증 요구 (specs/002 R6)
CRON_SECRET=

